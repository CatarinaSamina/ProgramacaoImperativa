int next_divisor(int x, int y)
{
	int result;
	for(int i = 1; i < x; i++)
		if(i > y)
			result = ceil(x % i = 0);
	  else
	  	result = -1;
	 return result;
}

int divisors(int x, int *a, int n)
{	
	a[result] = next_divisor(x,o);
	return result;
}

int abundância(int x)
{
	for(int i = 0; i < x; i++)
		if(x > i)
			result = next_divisor(x,0);
	return result;
}